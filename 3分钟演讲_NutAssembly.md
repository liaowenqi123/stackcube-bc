# NutAssembly 实验 - 3分钟演讲稿

---

## PPT 第1页：问题与发现

### 标题
**State-only Diffusion Policy 在精细操作任务上的性能边界探索**

### 左半部分：研究问题

**任务**：NutAssembly（螺母装配）
- 机器人抓取螺母并精确放置到 peg 上
- 成功条件：XY 对齐 < 3cm（毫米级精度）

**算法**：Diffusion Policy（state-only）
- 输入：44维状态向量（位置、姿态、速度）
- 输出：7维关节控制
- 数据：30K 样本，200 个演示

**研究问题**：
> 仅用状态信息能否完成精细操作？

### 右半部分：实验结果

**三个版本对比**

| 版本 | 模型 | 训练 | 成功率 | 平均回报 |
|------|------|------|--------|---------|
| V1 | 256×4 | 300轮 | 0% | 46.15 |
| V2 | 384×6 | 100轮 | 0% | - |
| V3 | 384×6 | 100轮 | 0% | - |

**放宽条件测试**（V1）

| XY 阈值 | 成功率 |
|---------|--------|
| < 3cm（官方） | 0% |
| < 15cm（放宽） | **20%** ✓ |

**关键发现**：模型学会了任务，但精度不够

---

## PPT 第2页：原因分析与结论

### 左半部分：失败原因分析

**详细诊断**（最接近成功时刻）
```
✓ 机械臂松开: r_reach=0.33 < 0.6
✓ Z 位置: 差 2.3cm
✗ XY 对齐: 差 9.7cm ← 核心问题
```

**三层原因**

1. **配置问题**（已解决 ✓）
   - 环境名称错误
   - 控制器不匹配
   - Reward shaping 未启用

2. **训练问题**（部分解决）
   - V2/V3 训练不充分（100 vs 300 轮）
   - 数据质量 > 模型复杂度

3. **算法局限**（根本原因 ❌）
   - State-only 信息不足
   - 缺少视觉和历史信息
   - **无法达到毫米级精度**

### 右半部分：对比与结论

**与文献对比**

| 方法 | 输入 | 成功率 |
|------|------|--------|
| BC (MLP) | State | 10-20% |
| BC (RNN) | State + History | 15-25% |
| Diffusion Policy | Image + History | 40-60% |
| **本项目** | **State only** | **0% / 20%*** |

*放宽条件

**State-only vs 完整版**

| 特性 | 完整版 | 本项目 |
|------|--------|--------|
| 输入 | 图像+历史 | 当前状态 |
| 输出 | 动作序列 | 单步动作 |
| 精度 | 毫米级 | 厘米级 |

**核心结论**

✅ **成功**：
- 算法实现正确（训练收敛）
- 学会了任务流程（抓取、移动、对齐）
- 达到厘米级精度（10-15cm）

❌ **局限**：
- 无法达到毫米级精度（< 3cm）
- State-only 信息不足

💡 **价值**：
- 明确了算法边界
- 指明改进方向（添加历史观测）

---

## 演讲稿（3分钟，约450字）

### 开场（30秒）
大家好，今天我分享一个"失败"的实验，但这个失败很有价值。

我们研究的问题是：**Diffusion Policy 在只用状态信息的情况下，能否完成精细操作任务**？任务是 NutAssembly，机器人需要把螺母精确放到 peg 上，要求 XY 对齐误差小于 3 厘米。

### 实验结果（1分钟）
我们测试了三个版本的算法，用了 30,000 个训练样本。结果是：**官方条件下成功率全是 0%**。

但是，当我们放宽条件到 15 厘米时，V1 模型达到了 **20% 成功率**，平均回报 46。这说明什么？**模型学会了任务，只是精度不够**。

通过详细诊断，我们发现最接近成功时，机械臂能正确松开，Z 位置只差 2.3 厘米，但 **XY 对齐差了 9.7 厘米**，而任务要求 3 厘米。

### 原因分析（1分钟）
我们分析了三层原因：

第一层是**配置问题**，比如环境名称错误、控制器不匹配，这些都解决了。

第二层是**训练问题**，V2 和 V3 因为只训练了 100 轮，反而不如训练 300 轮的 V1。这告诉我们：**训练充分性比模型复杂度更重要**。

第三层是**根本原因**：State-only 的信息不足。原论文的 Diffusion Policy 用图像加历史观测，能达到 40-60% 成功率。我们只用当前状态，就像只给司机当前位置坐标，没有摄像头和 GPS，自然无法达到毫米级精度。

### 结论（30秒）
所以，0% 成功率不代表失败，而是**揭示了算法边界**：

- State-only Diffusion Policy 能达到**厘米级精度**（10-15cm）
- 但无法达到**毫米级精度**（< 3cm）
- 要突破需要添加历史观测或视觉信息

这个发现为未来改进指明了方向。谢谢！

---

## 关键话术（背下来）

1. **开场金句**：
   > "今天我分享一个'失败'的实验，但这个失败很有价值"

2. **核心发现**：
   > "模型学会了任务，只是精度不够"

3. **关键数据**：
   > "XY 对齐差 9.7cm，要求 3cm"

4. **根本原因**：
   > "State-only 信息不足，就像只给司机坐标，没有摄像头"

5. **结论金句**：
   > "0% 不代表失败，而是揭示了算法边界"

---

## PPT 设计建议

### 第1页布局
```
┌─────────────────────────────────────┐
│ State-only Diffusion Policy 性能边界 │
├──────────────┬──────────────────────┤
│ 研究问题      │  实验结果             │
│              │                      │
│ [任务图]      │  [结果表格]           │
│ NutAssembly  │  V1/V2/V3 对比       │
│              │  放宽条件测试         │
│ 成功条件：    │                      │
│ XY < 3cm     │  关键发现：           │
│              │  学会了但精度不够     │
└──────────────┴──────────────────────┘
```

### 第2页布局
```
┌─────────────────────────────────────┐
│      原因分析与结论                  │
├──────────────┬──────────────────────┤
│ 失败原因      │  对比与结论           │
│              │                      │
│ 1.配置问题✓  │  [文献对比表]         │
│ 2.训练问题△  │                      │
│ 3.算法局限✗  │  [完整版对比]         │
│              │                      │
│ 详细诊断：    │  核心结论：           │
│ XY差9.7cm    │  ✓ 厘米级精度         │
│ (要求3cm)    │  ✗ 毫米级精度         │
│              │  💡 明确了边界        │
└──────────────┴──────────────────────┘
```

### 视觉元素
- ✅ 用红色标注失败（0%）
- ✅ 用绿色标注成功（20%）
- ✅ 用箭头指向关键数据（9.7cm vs 3cm）
- ✅ 如果可能，放一张机器人尝试抓取的 GIF

---

## 时间控制

- 0:00-0:30 开场 + 问题介绍
- 0:30-1:30 实验结果 + 关键发现
- 1:30-2:30 原因分析（三层）
- 2:30-3:00 结论 + 价值

**练习建议**：
1. 计时练习 5 遍
2. 每个数字都要准确（9.7cm, 3cm, 20%）
3. 最后 30 秒一定要留给结论

---

**记住**：3 分钟要讲清楚"做了什么、发现了什么、为什么失败、有什么价值"。重点在**发现**和**价值**，不是失败本身！

祝演讲成功！🎤
